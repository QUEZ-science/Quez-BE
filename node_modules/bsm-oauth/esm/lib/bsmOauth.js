var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import axios, { isAxiosError } from 'axios';
import { ResourceMapper } from './resourceMapper.js';
import { ErrorHandler } from './errorHandler.js';
export default class BsmOauth {
    constructor(clientId, clientSecret) {
        this.BSM_AUTH_TOKEN_URL = 'https://api-auth.bssm.app/api/oauth/token';
        this.BSM_AUTH_RESOURCE_URL = 'https://api-auth.bssm.app/api/oauth/resource';
        this.bsmAuthPayload = {
            clientId,
            clientSecret
        };
    }
    getToken(authCode) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                return (yield axios.post(this.BSM_AUTH_TOKEN_URL, Object.assign(Object.assign({}, this.bsmAuthPayload), { authCode }))).data.token;
            }
            catch (error) {
                isAxiosError(error) && ErrorHandler.handleToken(error);
                throw error;
            }
        });
    }
    getResource(token) {
        return __awaiter(this, void 0, void 0, function* () {
            const resource = yield this.getRawResource(token);
            return ResourceMapper.toResource(resource);
        });
    }
    getRawResource(token) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                return (yield axios.post(this.BSM_AUTH_RESOURCE_URL, Object.assign(Object.assign({}, this.bsmAuthPayload), { token }))).data.user;
            }
            catch (error) {
                isAxiosError(error) && ErrorHandler.handleResource(error);
                throw error;
            }
        });
    }
}
//# sourceMappingURL=bsmOauth.js.map